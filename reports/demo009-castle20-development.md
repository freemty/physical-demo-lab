# demo009：20 件 Isaac 城堡续作账本

2026-09-06。状态：进行中；当前优先完成 20 件，200/1000 件暂停。
用户要求后续直接在服务器 23 的本仓库开发、记录关键待办并提交推送。

## 继承证据，不重写失败

原运行根为 `/data1/ybyang/physical-demo-lab-runtime/outputs/castle-isaac`。
`goal-dev-v3-pgs4` 仅通过预摆稳定性；`expert-dev-v4-pgs4` 实际只完成
left_foot、right_foot 两块，在第三块 left_wall 下探因手部误差 27.173 mm 停止。
这不是完整搭建。原始结果、逐步轨迹和视频均保留。

原实现来自真正的 Git 工作树
`/data1/ybyang/physical-demo-lab-runtime/worktrees/isaac-ports-20260906`。
本次只引入城堡专用源文件、不可变蓝图、测试与规格，不覆盖已验收的公共运行时。
200/1000 件名为 worktrees 的目录实际只是运行副本，不能与此 Git 工作树混淆。

## 本轮控制候选

从失败末帧重新计算 FK，与原生手部位置差 0.201 微米、姿态差 5.26e-7 rad。
有界数值逆解能达到同一端点，最小关节限位余量约 0.197 rad；
但它需要显著改变多个关节，不证明原路径可执行或无碰撞。
不能把原失败直接解释为不可达或单一关节撞到限位。

候选采用较低阻尼的 Jacobian 位置驱动目标，并限制单次目标相对实际关节的误差，
在每段末端加入最多 3 秒有界收敛等待。不是瞬移、不直接设运行中关节/积木状态；
没有提高驱动力，也没有放宽 15 mm 动作停止门槛或原物理验收。
多项控制变化一起测试，不能把通过归功于某一个参数。
专用运行时采用 GPU UUID 屏蔽、逻辑 CUDA0 / renderer 物理编号的映射，
与其他已完成 demo 的运行时分离。每次仍须检查实际进程占卡。

## V5 三块验证（不是完整成功）

`expert-dev-v5-bounded-ik-three` 的三块均完成落位检查；第三块下探残差
3.926 mm，原 27.173 mm 停滞未在本次重现。三块最高抬升分别为
265.7 / 265.5 / 229.4 mm；抬升至少 80 mm 且双指均有非零法向力的
240 Hz 样本分别为 1785 / 1758 / 1656。此结果只证明当前组合修改在 seed 0 的前三块有效。

独立回执 `audit-v2.json` 核验 19848 个原生步、4962 个控制步、
2481 帧 / 82.7 秒视频；轨迹证据一致，完整任务成功仍为 false，退出码 2 符合部分运行。
全程接触汇总中的非手指穿透样本为 0，不等于不存在正间距预接触。
资源监控和过程退出回执均保留在同名前缀旁。

首次 `audit-v1.json` 错把带符号的法向力标量按正值筛选，导致夹持计数为 0。
SDK 的 `RigidPrim.get_contact_force_data` 返回原生法向标量，未承诺非负；
实际双指接触记录存在约 -3.5 N 等非零值。审计改为有限非零幅值，
添加负号受力单元测试；没有修改接触记录、运行控制器或原城堡验收器。
旧回执保留，新回执另存；这不是新的物理重跑或独立动力学复现。

复现环境解释器为 `/data1/ybyang/physical-demo-lab-runtime/venv/bin/python`，
不是主仓库内不存在的 `venv/bin/python`。
12 项城堡单元测试通过。下一轮 `expert-dev-v6-bounded-ik-full` 使用相同控制器、
seed 0、PGS / 4 velocity iterations、20 件、50000 控制步、3600 秒墙钟上限，
GPU 1；完整结果见下一节。

## V6 首条完整装配通过

`expert-dev-v6-bounded-ik-full/audit-v1.json` 独立回放通过，
[仓库内原回执副本](demo009-first-full-audit.json)：
20/20 件均完成原生双指受力抬升和最终脱手支撑，105644 个 240 Hz 原生步、
26411 个 60 Hz 控制步，模拟时间 440.1833 秒。最终连续稳定 11.2333 秒。
20 件最高抬升范围 211.8–265.7 mm，每件离桌至少 80 mm 且双指有非零法向力
的原生样本至少 1472 个。最大接触穿透 0.871 mm；全程非手指穿透样本计数 0。

独立进程退出 0，监控 2419 次未发现本进程组跨卡；
完整视频 13206 帧 / 440.2 秒 / 640×480，已全片解码并查看最后一帧。
视频比物理时钟多半个 30 Hz 帧以内，符合采样约定。
源码快照和蓝图哈希与 V5 一致，未在试验途中修改控制参数。

`final-v6-negative-one-step` 仅允许一个控制步，实际以
`step_budget_exhausted` 失败，退出 2；独立回放 4 个原生步、零抬升、
完整装配 false。GPU 7 的单卡映射在这个短运行中经进程组监控确认。
`final-v6-loose` 完成 6 秒散放初态测试，最大漂移约 1.16e-9 m，
退出 0、初态仍未解决、完整装配 false；独立回放 1440 个原生步和 180 视频帧通过。

## 固定版本验收正在执行

控制器等源文件冻结为提交 `e6d2b9f` 中的版本。另跑
`final-v6-seed0` 至 `final-v6-seed5`，不把开发首跑当作六种子首轮之一。
统一 seed 0–5、PGS、velocity iterations 4、20 件、50000 控制步、
640×480 全程视频；每实例墙钟上限 5400 秒，仅因并行资源调度延长，
没有改变物理预算或成功谓词。最多两实例，分别用当时空闲的 GPU 1、7；
启动器会拒绝已占卡，并持续检查自身进程组的 CUDA 上下文。

seed 0–3 首轮均已通过完整装配、独立审计和全视频解码，分别保留
[seed 0 回执](demo009-seed0-audit.json)、[seed 1 回执](demo009-seed1-audit.json)、
[seed 2 回执](demo009-seed2-audit.json)、[seed 3 回执](demo009-seed3-audit.json)。
四轮均为 20 个原生双指受力抬升，全部源文件/蓝图哈希与开发首跑一致；
已查看各自最终画面，所记录的积木-过滤对象接触中非手指穿透计数均为 0。

| seed | 原生步 / 控制步 | 连续稳定秒 | 视频帧 / 秒 | 进程 |
| --- | --- | --- | --- | --- |
| 0 | 105644 / 26411 | 11.2333 | 13206 / 440.2 | 0 |
| 1 | 105720 / 26430 | 11.2708 | 13215 / 440.5 | 0 |
| 2 | 105756 / 26439 | 11.0250 | 13220 / 440.6667 | 0 |
| 3 | 105552 / 26388 | 11.2333 | 13194 / 439.8 | 0 |

补充单指重复记录、非有限法向力不得作为双指受力的负向测试；14 项城堡测试通过。
仅改测试，没有改变冻结运行代码或审计器。

当前 seed 4/5 正在执行，已确认 4 次成功、0 次失败。至少 5/6 成功、
至少三条完整成功视频以及全部审计/收尾完成前，demo009 仍为进行中。
200/1000 件不恢复。

## 关键待办及完成条件

- [x] 将旧 20 件实现接到服务器主仓库的独立开发分支，保留旧失败证据。
- [x] 通过第三块下探、真实夹持、抬升和释放；审查原生机器人接触记录。
- [x] 连续完成 20 件；原始目标、质量、几何、支撑关系不变。
- [x] 首条完整运行核对每件确实被接触抓起并搬运，不仅检查 hand 高度或出现 lift 标签。
- [ ] 固定成功版本执行 seeds 0–5，至少 5/6 完整通过，并有至少三个完整成功视频。
- [ ] 回放每个运行自己的哈希固定 verifier；检查全部 240 Hz 状态/接触与 60 Hz 命令时钟。
- [ ] 完成负向检查、视频帧数/时间和关键画面审查；不能仅凭 success 或退出码。
- [ ] 更新经验、双侧项目地图、demos 账本，完成仓库检查后提交推送。
- [ ] 20 件完成前不恢复 200/1000 件；其碰撞代理、实例偏移、宽板释放问题仍未解决。

验收继续遵循 [原规格](../docs/specs/demo009-block-castle.md)。
物理回执位于各输出目录的独立审计 JSON；本报告保留完整装配与多种子验收的边界。
